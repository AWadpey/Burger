<li>
	{{burger_name}}

	<button class="change-devour" data-id="{{id}}" data-newsleep="{{devoured}}">
		{{#if devoured}}Eating time!{{else}}Order more!{{/if}}
	</button>

	{{!-- <button class="delete-cat" data-id="{{this.id}}">DELETE!</button> --}}
</li>
